import leon.collection._
import leon.webDSL.webDescription._

object Main {
    //This function will be called by the evaluator. It has to return a WebPage
    def main() = {
      val header = Header(HLTwo(), "header")
        val w = WebPage(List(), List(header))
        w
    }
}
